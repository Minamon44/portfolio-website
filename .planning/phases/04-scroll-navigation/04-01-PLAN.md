# Plan 04-01: Scroll Navigation + Integration

**Phase:** 4 — Scroll Navigation + Integration
**Plan:** 04-01
**Goal:** Smooth scrolling, scroll-spy, mobile nav, cohesive single-page experience
**Requirements**: LAYOUT-01
**Depends on:** Phase 3 (Section Components)

---

## Task 1: Add Smooth Scrolling CSS

**Goal:** Enable native smooth scrolling with sticky nav offset

**Do:**
- Update `Website/src/app/globals.css`
- Add `scroll-behavior: smooth` to `html` element
- Add `scroll-padding-top: 56px` to `html` element

**Verify:**
- [ ] CSS compiles
- [ ] Smooth scrolling works when clicking nav links

---

## Task 2: Create useScrollSpy Hook

**Goal:** Detect currently visible section for active navigation state

**Do:**
- Create `Website/src/hooks/useScrollSpy.ts`
- Implement IntersectionObserver with proper rootMargin
- Return currently active section ID
- Handle cleanup on unmount

**Verify:**
- [ ] TypeScript compiles
- [ ] Hook returns correct section ID on scroll

---

## Task 3: Update Navigation Component

**Goal:** Add scroll-spy highlighting and mobile hamburger menu

**Do:**
- Update `Website/src/components/layout/Navigation.tsx`
- Import useScrollSpy hook
- Add active state styling to navigation links
- Add hamburger button for mobile (<768px)
- Implement mobile menu overlay with section links
- Toggle menu on hamburger click
- Close menu on link click or outside click

**Verify:**
- [ ] Active link highlights on scroll
- [ ] Hamburger visible on mobile
- [ ] Mobile menu opens/closes
- [ ] Links in mobile menu scroll to sections
- [ ] TypeScript compiles

---

## Task 4: Verify Page Composition

**Goal:** Ensure all sections render correctly with navigation

**Do:**
- Run build to verify no regressions
- Test navigation links scroll to correct sections
- Verify mobile menu functionality

**Verify:**
- [ ] `npm run build` succeeds
- [ ] `npm run lint` passes
- [ ] All navigation links work
- [ ] Mobile menu functions correctly

---

## Final Verification

- [ ] `npx tsc --noEmit` — TypeScript compiles
- [ ] `npm run lint` — ESLint passes
- [ ] `npm run build` — Build succeeds
- [ ] Smooth scrolling works with 56px offset
- [ ] Scroll-spy highlights active section
- [ ] Mobile hamburger menu functions
- [ ] All sections accessible via navigation
