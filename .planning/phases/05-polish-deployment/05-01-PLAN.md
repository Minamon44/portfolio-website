# Plan 05-01: Polish + Deployment

**Phase:** 5 — Polish + Deployment
**Plan:** 05-01
**Goal:** SEO, GitHub Pages deployment, performance, 404 handling
**Requirements**: DEPLOY-01, DEPLOY-02
**Depends on:** Phase 4 (Scroll Navigation + Integration)

---

## Task 1: Add SEO Metadata

**Goal:** Add comprehensive SEO metadata to layout.tsx

**Do:**
- Update `Website/src/app/layout.tsx`
- Add title template
- Add meta description
- Add Open Graph tags (title, description, type, URL)
- Add Twitter card metadata

**Verify:**
- [ ] Title appears in browser tab
- [ ] Description visible in page source
- [ ] Open Graph tags present
- [ ] TypeScript compiles

---

## Task 2: Create Custom 404 Page

**Goal:** Display styled 404 error with navigation back to home

**Do:**
- Create `Website/src/app/not-found.tsx`
- Display terminal-native styled error message
- Include link back to home page
- Match design system (JetBrains Mono, warm cream, ASCII markers)

**Verify:**
- [ ] 404 page renders when visiting invalid URL
- [ ] Link to home page works
- [ ] TypeScript compiles

---

## Task 3: Verify Deployment Configuration

**Goal:** Ensure Next.js config is correct for static export

**Do:**
- Verify `next.config.ts` has `output: 'export'`
- Verify `trailingSlash: true`
- Verify `images: { unoptimized: true }`
- Verify `.nojekyll` exists in public directory
- Verify deploy.yml workflow is correct

**Verify:**
- [ ] `npm run build` produces static export in `./out/`
- [ ] `.nojekyll` copied to `./out/`
- [ ] All assets in `./out/_next/`

---

## Task 4: Final Build Verification

**Goal:** Complete build and lint verification

**Do:**
- Run `npx tsc --noEmit`
- Run `npm run lint`
- Run `npm run build`
- Verify no warnings or errors

**Verify:**
- [ ] TypeScript compilation passes
- [ ] ESLint passes
- [ ] Build succeeds
- [ ] Static export generated

---

## Final Verification

- [ ] `npx tsc --noEmit` — TypeScript compiles
- [ ] `npm run lint` — ESLint passes
- [ ] `npm run build` — Build succeeds
- [ ] SEO metadata visible in page source
- [ ] Custom 404 page renders
- [ ] Static export in `./out/` directory
- [ ] `.nojekyll` in `./out/`
