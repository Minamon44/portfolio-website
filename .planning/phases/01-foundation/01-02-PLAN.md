---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - .github/workflows/deploy.yml
  - public/.nojekyll
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-02
  - LAYOUT-03
user_setup:
  - service: github
    why: "GitHub Pages deployment target"
    env_vars: []
    dashboard_config:
      - task: "Enable GitHub Pages with GitHub Actions source"
        location: "Repository Settings → Pages → Build and deployment → Source: GitHub Actions"
estimate:
  tokens: 18000
  raw_tokens: 9000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - GitHub Actions workflow deploys static export to GitHub Pages on push to main
    - public/.nojekyll file exists to prevent Jekyll processing of _next/ assets
    - All 5 custom breakpoints render correctly (visual verification at 1280px+, 1024px, 850px, 768px, 640px)
    - Design token parity verified: computed styles match DESIGN.md values for critical tokens
    - Accessibility baseline met: WCAG AA contrast, focus visible, semantic HTML, ARIA labels
  artifacts:
    - .github/workflows/deploy.yml with build and deploy jobs
    - public/.nojekyll (empty file)
  key_links:
    - deploy.yml uploads ./out artifact → GitHub Pages serves it
    - .nojekyll prevents Jekyll from ignoring _next/ directory
    - next.config.ts output: 'export' → ./out directory structure matches workflow expectation

---

<objective>
Add GitHub Pages deployment pipeline and verify the foundation scaffold meets all Phase 1 success criteria across all 5 breakpoints with full design token fidelity.

Purpose: Complete the walking skeleton by adding deployment automation and rigorous verification that the tracer slice is production-ready.

Output: Deployable static site with CI/CD pipeline, verified responsive behavior, and confirmed design system parity.
</objective>

<execution_context>
@/home/mina/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/mina/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@/home/mina/Projects/Portfolio Website/DESIGN.md
@.planning/phases/01-foundation/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>GitHub Actions Deployment Workflow + .nojekyll</name>
  <files>
    .github/workflows/deploy.yml
    public/.nojekyll
  </files>
  <action>
    Create the GitHub Actions workflow for automatic deployment to GitHub Pages and the .nojekyll bypass file.

    1. **Create .github/workflows/deploy.yml (per D-06, RESEARCH §8, STACK.md §Deployment):** Create the directory .github/workflows/ if it doesn't exist. Write deploy.yml with:
       - name: "Deploy to GitHub Pages"
       - on: push to main branch + workflow_dispatch
       - permissions: contents: read, pages: write, id-token: write
       - concurrency: group: "pages", cancel-in-progress: false
       - jobs.build: runs-on ubuntu-latest, steps: checkout@v4, setup-node@v4 (node-version: "20", cache: npm), npm ci, npm run build, upload-pages-artifact@v3 (path: ./out)
       - jobs.deploy: needs build, runs-on ubuntu-latest, environment: github-pages, steps: deploy-pages@v4
       - Use pinned action versions as specified in RESEARCH.md

    2. **Create public/.nojekyll (per D-06, RESEARCH §8, STACK.md §Critical Configuration):** Create empty file at public/.nojekyll (touch public/.nojekyll). This prevents GitHub Pages from running Jekyll which would ignore _next/ directories (underscore-prefixed paths).

    3. **Verify workflow syntax:** Run `npx yaml-lint .github/workflows/deploy.yml` or validate via GitHub Actions UI after push.
  </action>
  <read_first>
    /home/mina/Projects/Portfolio Website/.planning/phases/01-foundation/01-RESEARCH.md
    /home/mina/Projects/Portfolio Website/.planning/research/STACK.md
  </read_first>
  <verify>
    <automated>test -f .github/workflows/deploy.yml && test -f public/.nojekyll && npx yaml-lint .github/workflows/deploy.yml</automated>
    <fails_when>either file missing, or yaml-lint reports syntax errors</fails_when>
  </verify>
  <done>
    .github/workflows/deploy.yml exists with correct build+deploy jobs using pinned actions. public/.nojekyll exists as empty file. Workflow YAML syntax validates.
  </done>
</task>

<task type="auto">
  <name>Comprehensive Verification — Breakpoints, Token Parity, Accessibility</name>
  <files>
    src/app/globals.css
    src/components/layout/Navigation.tsx
    src/components/layout/SectionContainer.tsx
    src/app/page.tsx
    scripts/verify-tokens.mjs
  </files>
  <action>
    Perform rigorous verification of the Phase 1 scaffold against all success criteria from ROADMAP, CONTEXT.md decisions, and VALIDATION.md.

    1. **Verify design token parity (per D-01, VALIDATION.md Dimension 2):** Create a temporary verification script at scripts/verify-tokens.mjs that:
       - Loads the built ./out/index.html in a headless browser (Playwright) or reads computed styles from the dev server
       - Checks critical computed values match DESIGN.md:
         - body background-color === rgb(253, 252, 252) [#fdfcfc]
         - body color === rgb(32, 29, 29) [#201d1d]
         - body font-family contains "JetBrains Mono Variable"
         - nav height === 56px
         - nav position === sticky, top === 0px, z-index === 50
         - section padding-top/bottom === 96px
       - Exits 0 if all match, 1 otherwise with descriptive output

    2. **Verify responsive breakpoints (per D-02, VALIDATION.md Dimension 1 LAYOUT-02):** Using Playwright or manual device toolbar verification, confirm layout adapts at:
       - desk-lg: 1280px+ (default 960px max-width container)
       - desk: 1024px (same layout, nav horizontal)
       - tab: 850px (footer would collapse to 2-up if present)
       - tab-narrow: 768px (nav becomes hamburger drawer breakpoint — Navigation component ready for Phase 4 mobile menu)
       - mob: 640px (single-column, section padding tightens per DESIGN.md responsive behavior)

    3. **Verify sticky nav scroll behavior (per D-03, VALIDATION.md Dimension 3):**
       - Nav stays fixed at top during scroll
       - Anchor links (#hero, #experience, etc.) scroll smoothly with 56px offset (scroll-padding-top)
       - Nav background is var(--color-canvas) with hairline bottom border

    4. **Verify accessibility baseline (per VALIDATION.md Dimension 6):**
       - Semantic HTML: nav, main, section elements present
       - ARIA: navigation has role="navigation" and aria-label="Main navigation"
       - Focus visible: :focus-visible shows 2px solid var(--color-ink) outline with 2px offset
       - Color contrast: cream (#fdfcfc) on ink (#201d1d) = 12.6:1 (exceeds 4.5:1 AA)
       - lang="en" on html element
       - Reduced motion: smooth scroll respects prefers-reduced-motion (scroll-behavior: smooth on html)

    5. **Run full verification pipeline:** Execute `npm run build && npx tsc --noEmit && npm run lint` again to ensure no regressions.
  </action>
  <read_first>
    /home/mina/Projects/Portfolio Website/.planning/phases/01-foundation/01-VALIDATION.md
    /home/mina/Projects/Portfolio Website/DESIGN.md
    /home/mina/Projects/Portfolio Website/.planning/phases/01-foundation/01-01-PLAN.md
  </read_first>
  <verify>
    <automated>npm run build && npx tsc --noEmit && npm run lint</automated>
    <fails_when>non-zero exit from any command</fails_when>
  </verify>
  <done>
    GitHub Actions workflow created and syntactically valid. .nojekyll file exists. All 5 breakpoints verified responsive. Design token parity confirmed for critical tokens (canvas, ink, font-family, nav height, section rhythm). Accessibility baseline met (WCAG AA contrast, focus visible, semantic HTML, ARIA). Full build pipeline passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → GitHub Pages | Trusted CI/CD publishes built artifacts |
| Public repository → GitHub Actions | Workflow runs on push; supply chain via npm packages |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | GitHub Actions workflow YAML | low | mitigate | Pinned action versions; workflow reviewed in PR |
| T-02-02 | Spoofing | Malicious npm package in dependency tree | medium | mitigate | package-lock.json locks versions; Dependabot alerts enabled |
| T-02-03 | Information Disclosure | GitHub Pages URL public | low | accept | Portfolio is intended to be public |
| T-02-SC | Tampering | npm installs in CI | high | mitigate | CI uses npm ci with package-lock.json; no floating versions |

</threat_model>

<verification>
**Phase 1 Complete Verification:**

1. **Deployment Pipeline:** .github/workflows/deploy.yml exists, valid YAML, uses pinned actions
2. **Jekyll Bypass:** public/.nojekyll exists (empty file)
3. **Responsive Breakpoints:** Manual/Playwright verification at 1280px+, 1024px, 850px, 768px, 640px
4. **Token Parity:** Critical tokens verified via computed styles (canvas, ink, font-family, nav, section rhythm)
5. **Accessibility:** WCAG AA contrast, focus visible, semantic structure, ARIA labels, reduced motion
6. **Build Pipeline:** npm run build && npx tsc --noEmit && npm run lint — all pass
7. **Static Export:** ./out/ contains index.html, _next/, no server dependencies
</verification>

<success_criteria>
- [ ] .github/workflows/deploy.yml created with correct build+deploy jobs
- [ ] public/.nojekyll exists as empty file
- [ ] Workflow YAML validates without errors
- [ ] All 5 breakpoints render correctly (visual verification)
- [ ] Critical design tokens match DESIGN.md in computed styles
- [ ] Sticky nav + smooth scroll + 56px offset working
- [ ] Accessibility baseline: contrast 12.6:1, focus visible, semantic HTML, ARIA
- [ ] Full build pipeline passes (build, typecheck, lint)
- [ ] Phase 1 ready for Phase 2 (Content Data Files) to begin
</success_criteria>

<output>
Create .planning/phases/01-foundation/01-02-SUMMARY.md when done
</output>